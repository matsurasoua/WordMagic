name: word_magic
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_launcher_icons: ^0.9.2  # 追加
  flutter_lints: ^2.0.0

flutter_icons:   # 追加
  android: true  # 追加
  ios: true      # 追加
  image_path: "assets/images/heiho.jpg" # 追加
  adaptive_icon_background: '#ffffff'
  adaptive_icon_foreground: "assets/images/heiho.jpg"

flutter:
  uses-material-design: true
